name: GHTranscribe
options:
  bundleIdPrefix: com.guyharrison
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
targets:
  GHTranscribe:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: GHTranscribe
    configFiles:
      Debug: Local.xcconfig
      Release: Local.xcconfig
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.guyharrison.ghtranscribe
        PRODUCT_NAME: GHTranscribe
        SWIFT_VERSION: 5.0
        TARGETED_DEVICE_FAMILY: "1"
        GENERATE_INFOPLIST_FILE: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_CFBundleDisplayName: GHTranscribe
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait
        INFOPLIST_KEY_NSCalendarsFullAccessUsageDescription: "GHTranscribe checks your calendar for an event overlapping a recording's time, to use as its title."
        CODE_SIGN_STYLE: Automatic
